# Critical
AVD-AWS-0104 # (CRITICAL): Security group rule allows egress to multiple public internet addresses.
AVD-AWS-0095 # Topic does not have encryption enabled.

# High
AVD-AWS-0015 (HIGH): CloudTrail does not use a customer managed key to encrypt the logs.

# Medium
AVD-AWS-0178 # (MEDIUM): VPC does not have VPC Flow Logs enabled.

# Low
AVD-AWS-0089 # (LOW): Bucket has logging disabled
AVD-AWS-0017 # (LOW): Log group is not encrypted.
